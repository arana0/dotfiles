git init
    .gitなどを作成しバージョン管理をできるようにする

git add <file>
    gitで管理するものに<file>を指定する
    .でディレクトリ以下すべてのファイル

git commit -m "message"
    変更を(localに)記録する
    -m "message"はしていしないといけない

git remote add origin https://github.com/user_name/repository
    originをhttps://github.com/user_name/repositoryに保存する
    originはただの名前だが通常はoriginになっているらしい

git push -u origin master
    originをbranch masterとしてネット上にアップする
    masterはただのbranch名だが通常はmasterになっているらしい
    
